{
    "name": "CycloneDX CLI",
    "id": "cyclonedx",
    "version": "1.0.0",
    "description": "cyclonedx is a command-line tool for working with Software Bill of Materials (SBOM). It can perform various operations on SBOMs, such as analysis, modification, diffing, merging, format conversion, signing and verification.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of cyclonedx to install. Accepts versions with 'v' prefix."
        }
    }
}
