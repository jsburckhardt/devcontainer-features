{
    "name": "Azure Development CLI",
    "id": "azd",
    "version": "1.0.0",
    "description": "Azure Developer CLI (azd) is an open-source tool that accelerates the time it takes for you to get your application from local development environment to Azure.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of azd to install. Accepts versions with 'v' prefix."
        }
    }
}
